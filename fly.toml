app = "musicbot-telegram"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  SQLITE_CACHE_PATH = "./data/cache.db"
  MAX_QUEUE_SIZE = "50"
  COMMAND_COOLDOWN = "3"

[mounts]
  source = "data"
  destination = "/app/data"
